import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  profileNameText: {
    fontFamily: "spaceGrotesk700",
    fontSize: 20,
    color: "#FFFFFF",
    textAlign: "center",
    marginTop: 48,
  },
});

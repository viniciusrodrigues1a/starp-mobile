import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  labelText: {
    fontFamily: "spaceGrotesk500",
    fontSize: 16,
    color: "#FFFFFF",
  },
  emailText: {
    fontFamily: "spaceGrotesk500",
    fontSize: 16,
    color: "#767676",
  },
});

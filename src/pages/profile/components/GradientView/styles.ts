import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  containerView: {
    paddingTop: 48,
    paddingBottom: 32,
    paddingHorizontal: 24,
    marginBottom: 32,
  },
});
